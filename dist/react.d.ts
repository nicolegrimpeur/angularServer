import express from 'express';
export declare const react: {
    site: typeof site;
};
declare function site(req: express.Request, res: express.Response, base: string, dirname: string, listePages: Array<string>): void;
export {};
